export enum AccidentStatus {
  P = 'Pending',
  A = 'Approved',
}

export enum UserRoles {
  SA = 'Super Admin',
  P = 'Partner',
  H = 'Host',
  CO = 'Car Owner',
}

export enum PlatformAccessStatus {
  P = 'Pending',
  A = 'Approved',
  R = 'Rejected',
  B = 'Blocked',
}


